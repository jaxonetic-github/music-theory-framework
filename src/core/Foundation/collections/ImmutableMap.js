export class ImmutableMap{constructor(e=[]){this._entries=Object.freeze([...new Map(e).entries()].map(Object.freeze));Object.freeze(this)}get size(){return this._entries.length}get(k){return this._entries.find(([x])=>Object.is(x,k))?.[1]}has(k){return this._entries.some(([x])=>Object.is(x,k))}set(k,v){const m=new Map(this._entries);m.set(k,v);return new ImmutableMap(m)}delete(k){const m=new Map(this._entries);m.delete(k);return new ImmutableMap(m)}[Symbol.iterator](){return this._entries[Symbol.iterator]()}}export default ImmutableMap;
