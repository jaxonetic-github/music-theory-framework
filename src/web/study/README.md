# Technical Studies Web

The v9.1 panel receives study definitions, roots, traversal policies, measures-per-system choices, progression realizations, harmonic rhythms, annotation policies, and active progression choices from Web bootstrap. React owns draft controls, capacity presentation, stale-operation protection, and responsive display only. Two octaves, selected key, canonical traversal, and four semantic measures per system are the initial draft. Material changes invalidate pending work and retain completed output as stale.

Core Study expansion creates the authoritative `ExerciseSetRequest`; `ExerciseSetApplication`, conventional Rendering, and Publishing remain the execution boundaries. Responsive Layout may introduce additional visual systems without changing requested semantic grouping. The panel contains no Theory, voicing, spelling, notation, pagination, download, or audio algorithms.
